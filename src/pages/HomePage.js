import React from 'react';
import MainContent from "../components/MainContent";

const HomePage = () => {
    return (
        <div>
            <MainContent/>
        </div>
    );
};

export default HomePage;